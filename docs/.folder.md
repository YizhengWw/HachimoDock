# Folder Plan: docs

<!-- vibe-folder-map:start -->
## Architecture
- Scope: docs
- Function: Product, planning, and developer documentation for the Pet Manager configuration, two-method device binding, final onboarding agent appearance confirmation, local agent detection, agent-first Codex-to-Westie defaults, draft-before-apply channel changes, voice routing plus board voice enable/trigger-button configuration, USB OTA affordance, compressed appearance state video/WAV asset sync with default done/error/waiting_user fallback and user overrides, one-click board-audio preflight, fast low-resolution black-background 4:3 generation defaults, required-marker-prop generation prompts with screen-touch feedback semantics, channel management from both the device page and appearance gallery, and local dual-end developer setup. Runtime prompt WAV assets live with bundled appearance assets under `ref/public/` rather than as planning docs.
- Sync: if this folder changes, update this file immediately.

## Files
| File | Pos | Function |
|---|---|---|
| `developer-setup_zh_Hans.md` | `doc` | Chinese local developer setup guide for the desktop `ref/` app and board runtime, with Radxa Cubie A7Z as the default first-replica target for showing the project on the SPI screen; includes Windows/macOS prerequisites, install, build, test, the mandatory A7Z first-boot Wi-Fi pause before SSH deployment, MQTT URL handoff through the local broker/bridge script, dual-end debugging commands, deployment checks, and common troubleshooting |
| `pet-claw-lite-admin.md` | `doc` | Product draft for the Pet Manager workbench, covering Ethernet/Wi-Fi device binding, final onboarding agent appearance confirmation, first-level appearance gallery plus dashboard/gallery agent-to-appearance management, Codex-bound Westie defaults, local-first agent detection, draft-before-apply channel persistence, fast low-resolution black-background 4:3 generation defaults, distinct generated-pet action prompts with required marker props and screen-touch feedback semantics, achievements, token stats, dress-up flows, and companion sync boundaries |
| `superpowers/` | `dir` | Design specs and implementation plans created by the Superpowers workflow |
| `ui-style-guide.md` | `doc` | UI 风格规范与 CSS 变量约束，定义全局颜色、字体、层级、组件状态与落地步骤 |
| `voice-architecture.md` | `doc` | Voice routing contract for Pet Manager, voice-service-node, and OpenClaw runtime, including agent-session-bus injection, selected-agent/session behavior, board voice enable/trigger-button configuration marked for USB OTA, and board-audio enable preflight that starts Bridge/voice-service before sending audio_bridge to the active board |
<!-- vibe-folder-map:end -->
