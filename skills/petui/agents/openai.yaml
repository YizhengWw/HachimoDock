interface:
  display_name: "petui"
  short_description: "生成、校验并发布 ESP32-P4 桌搭子工具与小游戏组件"
  default_prompt: "Use $petui to generate, validate, and publish a new P4 desktop-pet component."

policy:
  allow_implicit_invocation: true
