# Folder Plan: .github

<!-- vibe-folder-map:start -->
## Architecture
- 2026-08-04 Git LFS source-check correction: Windows and macOS checkout now
  materialize the tracked P4 H.264/WAV assets before desktop resource
  preparation, and changes to `prepare-p4-ready-assets.mjs` trigger the same
  validation workflow instead of leaving the packaging path unchecked.
- 2026-07-27 desktop packaging update: matching macOS and Windows runners prepare real target-native Node plus pinned/checksummed FFmpeg runtimes and notices, run frontend/bridge regression tests, build Tauri packages, and publish installer artifacts.
- Scope: .github
- Function: GitHub automation and instruction mirror for this workspace.
- Sync: if this folder changes, update this file immediately.

## Files
| File | Pos | Function |
|---|---|---|
| `copilot-instructions.md` | `doc` | GitHub Copilot instructions mirrored from the workspace contract |
| `workflows/pet-manager-desktop-build.yml` | `ci` | Native macOS and Windows source-validation workflow with Git LFS-aware checkout, frontend/bridge tests, target resource preparation, and Rust checks |
<!-- vibe-folder-map:end -->
