# Folder Plan: scripts

<!-- vibe-folder-map:start -->
## Architecture
- 2026-08-03 reproducible staged Node reuse: desktop resource preparation
  prefers an existing target-compatible, relocatable `generated-runtime/node`
  after validating it again. This keeps repeated local packages independent of
  Homebrew PATH changes; clean builds still fail closed unless the active Node
  is relocatable or `PET_MANAGER_NODE_BIN` is explicitly supplied.
- 2026-08-03 macOS runtime packaging correction: desktop preflight rejects
  Node/FFmpeg Mach-O binaries that reference Homebrew, `@rpath`, or other
  unbundled dynamic libraries, preventing a packaged Bridge restart loop.
  P4 ready-pack preparation also reuses cached WAV payloads only when their
  recorded source SHA-256 still matches, preserving the factory-pinned pack
  across FFmpeg releases without accepting stale audio, and reports an
  actionable `git lfs pull` error before attempting to transcode LFS pointers.
- 2026-07-31 P4 v9 ready-pack update: the prebuilder reads FFmpeg container
  duration, exports at most 225 H.264 frames across the full timeline, emits
  exact per-family `durationMs`, and writes 640x480/up-to-15fps/CRF27 Annex-B
  streams with aspect-fit letterboxing, the ESP decoder-compatible SPS, and
  one x264 thread/slice per access unit for stable tinyh264 decoding.
  Desktop and factory packaging consume that same hashed pack, so installation
  and device synchronization never perform media conversion.
- 2026-07-30 Windows UTF-8 and long-path update: shared PowerShell setup forces UTF-8 console/Python/locale state, isolates temporary files under a short ASCII sibling cache, rejects non-ASCII or overlong PlatformIO roots before ESP-IDF extraction, and can persist the sanitized core for the current user. Both release/dev desktop launchers consume the same environment contract.
- 2026-07-27 desktop packaging update: Tauri resources are staged from the target host's real Node executable plus a pinned, checksum-verified target-native FFmpeg static build and license/source notices; the preflight rejects Git LFS pointers and wrong-platform binaries, installs only the bridge's locked production dependencies, and can produce a resource-complete current-user NSIS installer from a cargo-xwin build.
- 2026-08-03 local macOS TCC identity enforcement: `sign-macos-local-app.mjs` replaces Tauri's per-build CDHash designated requirement with a stable `com.petmanager.desktop` identifier requirement after local app bundling and can verify the exact requirement without mutating the app. `bundle-macos-local-dmg.mjs` then rebuilds the DMG from that exact signed app, skips Finder/AppleScript layout to avoid resource-busy mount failures, and mounts the final image read-only to verify its contained app, preventing Tauri's standalone bundle command from silently replacing the stable signature. `npm run build:mac:local` owns the complete sequence; production Developer ID signing remains separate.
- 2026-07-22 P4 widget smoke update: transformed runtime JSON stays compact, acknowledged widget chunks use the same three-attempt idempotent retry policy as the desktop client so high-baud CH343 frame loss does not create false component failures, and screenshots cover countdown-complete plus tomato-rest boundary states in addition to each initial view.
- 2026-07-28 widget commit timing update: hardware smoke installs keep the commit waiter open for 15 seconds, matching the desktop client and the board's flash-backed multi-component catalog sync time.
- Scope: scripts
- Function: Shell automation and guard checks for workspace consistency.
- Sync: if this folder changes, update this file immediately.

## Files
| File | Pos | Function |
|---|---|---|
| `build-windows-nsis-cross.mjs` | `script` | Reproducible macOS cross-bundler that validates and stages the x64 PE app, Windows Node/FFmpeg runtimes and notices, bridge, built-ins, generator skill, and embedded WebView2 bootstrapper into a current-user NSIS installer |
| `bundle-macos-local-dmg.mjs` | `script` | Rebuilds the local DMG from the exact stable-signed app, mounts the finished image read-only, and verifies that its contained app retains the stable TCC designated requirement |
| `dev-manager.ps1` | `script` | Canonical Windows Tauri dev launcher with inherited UTF-8 console and subprocess state plus an optional isolated Vite port |
| `dev-mqtt-broker.js` | `script` | Minimal local MQTT broker for development and Radxa recovery flows, including retained messages and QoS1 acknowledgements used by bridge/board diagnostics |
| `docs-check.sh` | `script` | Guard script that checks doc references and folder contracts while ignoring URLs and markdown anchors |
| `open-manager.ps1` | `script` | Windows launcher used by root open-manager.bat; initializes the shared UTF-8 environment, then focuses an existing release manager or builds and opens it |
| `prepare-desktop-resources.mjs` | `script` | Cross-platform Tauri preflight that stages target-native, relocatable Node/FFmpeg resources and bridge dependencies, rejects unbundled macOS dylib references, then generates or validates the built-in P4 ready pack before packaging |
| `prepare-p4-ready-assets.mjs` | `script` | Deterministically builds and verifies the shared install-time/factory Terrier P4 H.264 Annex-B/WAV pack using source-SHA-matched video/audio cache reuse and the fixed profile |
| `pre-commit-folder-check.sh` | `script` | Guard script that enforces folder contract presence before commit |
| `run-node-tests.mjs` | `script` | Cross-platform recursive `*.test.js` discovery across one or more roots and Node test-runner launcher used by the desktop package |
| `sign-macos-local-app.mjs` | `script` | Post-bundle local macOS signer/verifier with a `--verify-only` mode that enforces the exact stable bundle-id designated requirement so TCC Accessibility grants do not churn on every ad-hoc build |
| `start-hachimo-link.ps1` | `script` | Windows helper that starts the local MQTT broker and bridge with an auto-detected LAN broker URL, optionally syncing the Radxa board runtime over SSH |
| `test-p4-widgets.py` | `test` | Auto-detected USB hardware smoke test that installs every built-in P4 negative-screen widget, exercises its actions/ticks/stats, captures PNG screenshots, and restores the previously active widget |
| `watch-remote-cli-mqtt.cjs` | `script` | MQTT watcher for Pet Manager remote CLI binding diagnostics, streaming retained control messages and followed bridge state topics |
| `windows-utf8.ps1` | `script` | Shared Windows console/Python UTF-8 initializer, short sibling temp/core resolver with ASCII and portable-length guards, esptool locator, and optional current-user environment installer |
<!-- vibe-folder-map:end -->
