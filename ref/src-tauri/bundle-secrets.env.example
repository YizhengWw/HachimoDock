# Copy this file to `bundle-secrets.env` before packaging.
# `bundle-secrets.env` is intentionally git-ignored; do not commit real secrets.
PET_MANAGER_BUNDLED_MQTT_URL=mqtt://broker.openclaw.example:1883
PET_MANAGER_BUNDLED_MQTT_USERNAME=device
PET_MANAGER_BUNDLED_MQTT_PASSWORD=<MQTT_PASSWORD>
