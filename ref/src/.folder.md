# Folder Plan: ref/src

<!-- vibe-folder-map:start -->
## Architecture
- Scope: ref/src
- Function: React source for the Pet Manager desktop bridge configurator, split between setup, dashboard, first-level pet-album, and component-center shells, including a responsive component-center app store for negative-screen widgets with unified builtin/draft/create card geometry, install-time button-function customization across screen touch/top red button/front encoder, USB/SSH binding override downlink, coding-agent component generation prompts, generated draft install/delete flows, per-state appearance WAV prompt-sound configuration that compresses uploads to board-safe 16 kHz mono PCM, lets built-in and custom appearances store user overrides, persists a pending audio-OTA reminder after cue upload/removal with a top-right “下发音效” action backed by serialized Tauri appearance asset OTA, supports a preview-first appearance detail workspace with a nearby horizontal state rail, sticky current-state controls, and a lower contextual info drawer plus custom appearance single-state image+prompt regeneration from a closable detail-page modal that reuses the full custom-avatar wizard upload/config steps, keeps generation running after the modal is closed, replaces only the selected family video locally, and then reuses the existing appearance asset OTA to replace it on the board, and custom full-avatar generation skips importer-only directional `touch.right` / `touch.left` states while retaining them for imported/legacy records. It falls back to default `done.wav`, `error.wav`, and `waiting_user.wav` prompt cues, includes device guide copy for button customization, a dashboard status strip with manual USB serial rescan plus independent USB/WiFi status chips, shared form-control primitives, and a dashboard button-config area that gives the device callout SVG more room while keeping board/negative-screen button editing compact and sends the full visible button map through USB `button_config` OTA via `button_config_signal`, which only returns after board `button-config-ack` and retries once after reconnecting a stale USB writer. The dashboard voice section keeps session continuation, mock voice-text injection into the active agent session through the managed bridge, and board-originated voice action result status inside the shared shell using the same form rhythm. A new `shell/` subfolder hosts cross-page primitives (PageShell, Card, ContextRail, ToastProvider/useToast/ToastStack, DeviceContextProvider) consumed by `App.jsx`; DeviceContextProvider hydrates active channel state from the bridge profile before localStorage cache so stale browser state cannot falsely mark an unsynced desktop-pet appearance as current, owns the appearance USB sync progress so dashboard tab navigation does not reset an in-flight downlink display, while keeping WiFi availability separate from USB direct status for display and routing decisions.
- Sync: if this folder changes, update this file immediately. Plan 3 (2026-05-26) migrated `AppearanceGallery.jsx` onto the shared shell — header chrome flows through `<PageShell>`, USB/online/agent state through `useDeviceContext()`, and sync notifications through `useToast()`. On 2026-05-28, `DeviceDashboard.test.js` added coverage that wireless channel switching still reaches MQTT when a stale USB write reports `Connection lost`. On 2026-06-01, widget (negative-screen) USB OTA became ack-gated end-to-end — `install_widget_clawpkg` (src-tauri/usb_serial.rs) now waits for board `widget-install-ack` so the ComponentCenter success toast lines up with on-device activation; this is transparent at the React layer, but documented here to clarify why component-center install UX now feels synchronous instead of fire-and-forget.
- Reuse: `App.jsx` owns cached top-level shell switching; `ComponentCenter.jsx` consumes `BUILTIN_COMPONENT_CENTER` from `fixtures.js` for the builtin component catalog, `lib/component-generation-template.js` for agent prompt payloads, and `lib/clawpkg-contract.js` for the `.clawpkg` bundle contract and `COMPONENT_DASHBOARD_V1` slot schema; `DeviceSetup.jsx` and `DeviceDashboard.jsx` keep operational Tauri flows; appearance screens reuse shared helpers under `lib/`; `DeviceGuideModal.jsx` reads its 3-card content from `lib/device-guide-content.js` (screen mappings plus component-center button-function customization for screen touch, top red button, and front encoder); gallery and component-center action buttons reuse the shared accent/text-on-accent tokens in `styles.css`.

## Files
| File | Pos | Function |
|---|---|---|
| `main.jsx` | `entry` | React bootstrap that mounts the desktop bridge configurator |
| `App.jsx` | `component` | Shell router that swaps between the standalone setup wizard and cached bound-device/pet-album/component-center flows, uses a browser-only dev direct-dashboard fallback while keeping Tauri empty bindings on setup, refreshes DeviceContext after setup writes the selected channel/appearance and after avatar generation completes so dashboard appearance pickers see newly persisted shapes, preserves the source surface for appearance detail/help back navigation, and keeps first-level device, pet-album, and component-center sidebar tabs plus setup copy aligned to Ethernet or Wi-Fi binding |
| `ComponentCenter.jsx` | `component` | 组件中心：消费 Plan 1 shell（PageShell / Card / useToast / useDeviceContext），主区组件库网格（内置在前，草稿在后，徽章区分，草稿说明取 manifest description 而不是本地路径，卡片说明完整展示，自定义草稿卡片可直接删除），ComponentPreviewModal 作为最终安装确认并直接进入 USB/SSH OTA；内置组件先通过 Tauri `resolve_builtin_clawpkg_path` 解析包内 `builtin-clawpkgs/` 资源，避免依赖用户目录 `~/.openclaw/builtin-clawpkgs`，草稿仍直接使用 agent 生成的 on-disk path。"创建组件" actions 按钮打开右侧 CreateComponentDrawer 抽屉容纳 STEP 1/2/3，STEP 2 直接 `launch_agent_with_prompt` 调起当前跟随 agent，不再弹出 prompt-copy 文案弹窗，并明确“生成完成后组件中心会自动刷新并展示新草稿；没看到更新时，用 Step 3 拖拽或选择加入组件中心”。安装成功后写入 `pet-manager:active-component` 以驱动侧栏 ContextRail 更新。**应急/预防**：OTA success modal 提供"⤴ 返回主屏"（invoke `usb_set_screen_page("main")` → 设备端 `control/screen-page` topic，刚装完 widget 不想留在 stats 页时一键切回）；设备级应急控制（⤴ 设备返回主屏 / 🔄 强制同步形象）归 `DeviceDashboard` 的 `DashboardActionsMenu`，组件中心 actions 只保留"刷新草稿 + 创建组件"避免语义错位；安装/激活负一屏组件时调用 `applyRecommendedButtonConfigForWidget`（定义在 `DeviceDashboard.jsx`），自动把红钮设为负一屏主操作、旋钮设为负一屏调整并 USB OTA 下发（覆盖原音量/语音，toast 提示，可在设备页改回），让 widget 的 buttons.json 绑定开箱即生效；`CONTROL_OPTIONS` 的红钮短/长按已不再标记系统保留（`isSystemReservedControl` 随之删除）——设备端 `board_rotary_input.c` 现按“负一屏次优先”把指派给负一屏的控件以原生事件投递给 widget，仅 swipe 仍由 `screen_page.c` 截走切页（故不在可绑定列表）。详见 `docs/superpowers/specs/2026-06-01-negative-screen-button-routing-design.md`。 |
| `ComponentCenter.test.js` | `test` | Static Node coverage that keeps the component-center mock app-like with a responsive unified 组件库 grid, followed-agent prompt entry, Step 2 skill handoff guidance without a prompt-copy modal, custom/generated draft delete controls exposed from draft cards only, backend delete path guard and draft-description wiring, card picker, device preview, direct preview-modal OTA install path, hidden package documentation, create-drawer auto-refresh guidance plus manual drag-add fallback button, install-time button-function customization, and USB/SSH binding override wiring; asserts the obsolete dedicated drafts list under STEP 3 is gone |
| `DeviceDashboard.jsx` | `component` | 4-section device dashboard composed on the shared shell: PageShell + DashboardActionsMenu (actions slot, opt-in 📶 通过 USB 配 WiFi when USB connected) + DeviceStatusBar (区 1) + ChannelMatrixCard (区 2) + BoardButtonPanel (区 3, always visible) + Card.Collapsible<VoiceAssistantPanel> (区 4) + WifiApplyModal (USB-only modal for switching wifi over the already-stable serial channel, rendered with shared modal-card/form-control chrome). Pulls all device/USB/online/appearance/agent state from `useDeviceContext()` and surfaces success/error/progress notices through `useToast()`. Owns voice-config storage, voice-bus polling, managed-bridge mock button-text injection via `/mock-button-inject` without legacy-port fallback, USB `input/action` voice result listeners with transient reconnect handling, audio-bridge toggling, and `buildBoardButtonConfigBindings()` so the BoardButtonPanel rows become a complete USB `button_config` payload instead of only a voice-bridge command; button-config apply now attaches a request id and trusts the backend-held board `button-config-ack` result returned by `button_config_signal`, which reconnects and retries once if a stale USB writer accepts bytes that do not reach the board, before clearing dirty state or showing success. Exports the BOARD_BUTTON_CONTROL_ROWS / BUTTON_FUNCTION_OPTIONS constants for BoardButtonPanel to import, plus the shared `dispatchBoardButtonConfig` (single `button_config_signal` invoke, reused by the manual OTA button and the component-center install), `WIDGET_RECOMMENDED_BUTTON_ACTIONS` preset (红钮→负一屏主操作 / 旋钮→负一屏调整), and `applyRecommendedButtonConfigForWidget` (persists the preset + fires a `storage` event + OTAs with voice off); a `storage` listener re-reads voiceConfig so the panel reflects the component-center-applied preset live. **应急 wiring**：注入 `onDeviceReturnHome`（invoke `usb_set_screen_page("main")` → 设备端 `control/screen-page` topic，物理按键失灵切不回主屏时使用）、`onForceSyncAppearance`（读 `ACTIVE_APPEARANCE_KEY` localStorage 然后直 invoke `usb_sync_appearance`，绕过 `desktop-pet-assignment.js` 的 appearanceChanged cache，用于客户端 UI 与设备形象脱节时一键恢复），以及 `onApplyWifi`（USB 已连接时弹出 `WifiApplyModal` 走 `usb_apply_wifi` 切换板端 wifi）。 |
| `DeviceDashboard.test.js` | `test` | Static Node coverage that the dashboard composes PageShell + 4 cards in spec order (状态条 / 当前展示 / 按钮配置 / 语音助手折叠), pulls state from useDeviceContext (no local polling), requires full visible button-map USB OTA via `button_config_signal` with request-id + backend-held `button-config-ack` confirmation plus stale-writer reconnect retry, removes the old runtime/desktop-pet-channel panels, places DashboardActionsMenu in PageShell actions, places BoardButtonPanel inside a plain Card and VoiceAssistantPanel inside Card.Collapsible, keeps board voice action injection status wired, prevents stale legacy bridge injection fallback, guards appearance OTA to include user-overridden or default done/error/waiting_user WAV cue assets beside matching videos, verifies appearance chunk pacing against USB serial line speed, and guards wireless channel switching against stale USB write failures so MQTT remains available. |
| `DeviceSetup.jsx` | `component` | Device binding wizard with a lightweight Ethernet-card alternative above the primary Wi-Fi provisioning flow, including mock Ethernet detection, dev-only skip-provisioning walkthrough binding, connection-mode-aware retry and error copy after failures, bridge verification, detected-agent scanning, final single display-channel selection with default appearance preview, and a primary completed-state return action before entering the dashboard |
| `DeviceSetup.test.js` | `test` | Static Node coverage that keeps the onboarding entry as Ethernet-over-Wi-Fi cards without tabs, verifies the mock Ethernet binding path and dev skip-provisioning path, keeps retry/error copy on the failed binding method, requires final one-channel display selection with default appearance preview, and keeps the completed-state return button as the primary right-side action |
| `ProductExperience.test.js` | `test` | Static Node regression coverage for product-experience bugs around window sizing, first-level sidebar routing including the component center, fixed-height desktop sidebar behavior, Tauri setup-first routing, single desktop-pet assignment, dashboard guide entry, preview fallback, faster preview loading, flattened desktop HTTP bridge args, generation affordances, and Tauri packaged resource contracts for currently bundled runtime files with development path fallbacks kept debug-only |
| `usb-serial-widget-ota.test.js` | `test` | Static Node coverage for USB OTA transport regressions, including widget begin/commit ack gating and appearance asset transfer serialization so full sync and audio patch sync cannot overlap in the board's global staging tree |
| `AppearanceGallery.jsx` | `component` | Pet album rendered inside the shared `<PageShell>` — header actions collapse the three creation flows into a `SplitButton` and keep refresh as an independent action, each card surfaces a “使用中” badge driven by `useDeviceContext().currentDisplay`, sync results flow through `useToast()` instead of an inline floating banner, and the in-flight generation card sits inside a shell `<Card>`. `AppearanceChannelModal` is preserved (visually reworked to match shell tokens) so users can still pick a target channel from the gallery; USB / device-online / agent-appearance state is read entirely from `DeviceContextProvider` and applied through its `applyDesktopPet` wrapper. |
| `AppearanceGallery.test.js` | `test` | Static Node coverage that the gallery is rendered inside `<PageShell>` with refresh + `SplitButton` actions, the 3 creation labels live inside the SplitButton, USB / online / agent state comes from `useDeviceContext()` (no local polling), sync results go through `useToast()`, the in-flight task is wrapped in a shell `<Card>`, each card exposes the "使用中" badge + `is-active` border driven by `currentDisplay`, `onSetAsDesktopPet` is the rename of the old `onSelectChannel` callback, and `AppearanceChannelModal` is preserved with shell-token surface chrome plus the unchanged channel-selection + `ChannelSwitchConfirmModal` flow. |
| `AppearancePreview.jsx` | `component` | Resilient image/video preview element with lazy image loading, metadata-only idle video loading, asset URL fallback, source-image fallback, and controlled playback |
| `AppearanceDetail.jsx` | `component` | Appearance detail preview for persisted/built-in/codex-pet videos, normalized source labels, generated-family browsing through a preview-adjacent horizontal state rail, guarded deletion where allowed, sticky current-state audio/regeneration controls beside the preview, lower contextual source/model/path details, editable per-state WAV cue upload/removal with compression plus audio preview for built-in and custom appearances, and custom-only single-state regeneration opened from a compact clickable CTA button into a closable modal that reuses the full avatar wizard upload/config steps before running `runSingleFamilyVideo` in the background, persisting through `replaceFamilyVideo`, and exposing “替换到板端” through the existing appearance asset downlink |
| `AppearanceDetail.test.js` | `test` | Static Node test coverage that keeps detail focused on preview/navigation, deletion as a guarded action, codex pet source labeling, failed family materials hidden, preview-first workspace structure with sticky current-state controls and context drawer, configurable WAV cue UI wired through compression/default fallback, closable background single-state image+prompt regeneration, pending audio-OTA reminders, and appearance-store helpers |
| `ChannelSwitchConfirmModal.jsx` | `component` | Shared modal shown when the user manually switches the single followed device subject away from the current channel without implying a new appearance |
| `DeviceGuideModal.jsx` | `component` | 3-card device-control guide modal (screen-switch / control map / widget button behavior) — auto-opens once on first device bind from `DeviceDashboard.jsx`, reopens anytime via the low-key `?` icon in the dashboard hero, and uses static copy from `lib/device-guide-content.js` that explains component-center install-time button-function customization while keeping screen swipe as the stable page-switch gesture |
| `DeviceGuideModal.test.js` | `test` | Static Node coverage that the device guide distinguishes both red hardware controls and shows component-center button-function customization |
| `CustomAvatarWizard.jsx` | `component` | Custom avatar generation wizard that persists new appearances, exports reusable upload/config step components for single-state modal regeneration, keeps uploaded reference previews fixed-size and stable with FileReader data URLs, clarifies GIF first-frame input, adds field-level help bubbles, uses shared `avatar-pipeline/provider-config.js` persistence for API-key-only Volcengine Ark setup with Seedance 1.5-first product-fit model names, Seedance 2.0 activation guidance, and a custom fallback, defaults to fast low-resolution black-background 4:3 generation, and blocks incomplete generation config before submission |
| `CustomAvatarWizard.test.js` | `test` | Static Node regression coverage for reusable full-generation wizard steps, shared provider-config-backed Volcengine Ark API-key-only generation setup, fixed Ark address, Seedance 1.5-first default ordering, activation guidance, product-fit dropdown video model names, and fixed reference upload sizing |
| `CommunityImportHelp.jsx` | `component` | In-app standalone help page that explains codex pet sources, directory rules, and community import flow using bundled screenshots |
| `HelpTooltip.jsx` | `component` | Shared rounded-rectangle tooltip trigger used by help icons across the pet album and creation wizard |
| `fixtures.js` | `shared-data` | Builtin component-center catalog (`BUILTIN_COMPONENT_CENTER`: 摸鱼倒计时 / 番茄钟 / 喝水提醒 / Token 消耗, prompt builder, MagicMirror metadata, replacement preview, per-component button-function defaults) plus setup-flow scenario fixtures (`DEVICE_SETUP_FIXTURES`, `AGENT_DISCOVERY_FIXTURES`). Renamed from `mock-data.js` 2026-06-01 and trimmed of 10 unused PRD-era exports (MOCK_USER / MOCK_PACKS / MOCK_THEMES / MOCK_PET_STATS / MOCK_POSTCARDS / MOCK_SOUVENIRS / MOCK_TRAVEL_LOGS / INITIAL_PET_DRAFT / VIEW_IDS / LOGIN_SCENARIOS) — `MOCK_` prefix was misleading because the data is the actual product source of truth, not a placeholder. |
| `lib/clawpkg-contract.js` | `lib` | `.clawpkg` bundle structure constants, `COMPONENT_DASHBOARD_V1` 9-slot schema defining each negative-screen slot's id/label/role, and `validateClawpkgManifest` for checking required slot coverage |
| `lib/clawpkg-contract.test.js` | `test` | Node coverage for `.clawpkg` slot schema shape, all 9 `COMPONENT_DASHBOARD_V1` slots present, and manifest validation accepting/rejecting coverage |
| `lib/component-generation-template.js` | `lib` | Pure helper for resolving the currently followed coding-agent channel, building Codex / Claude Code / OpenClaw component-generation command payloads, negative-screen package prompts that allow screen touch, top red button, and front encoder button-function bindings, and ask for clarification when tap/long-press or requested hardware-button behavior is missing, MagicMirror/MMM conversion contracts, and natural-language prompt assembly |
| `lib/component-generation-template.test.js` | `test` | Node coverage for component-generation prompt shape, device-aligned button-function binding rules, button-configuration clarification wording, MagicMirror compatibility mapping, followed-channel resolution, and Codex / Claude Code / OpenClaw command payload selection |
| `lib/appearance-store.js` | `lib` | Cached app-local appearance manifest/video/source-preview/WAV cue persistence adapter that prepends built-in appearances, overlays built-in audio overrides from app-local storage, exposes synchronous cached listing plus force-refreshable async listing, writes/removes per-family audio cues, and falls back to built-in/default cue records if local storage cannot be read |
| `lib/appearance-store-cache.test.js` | `test` | Static Node coverage that keeps appearance listing cached, synchronously readable when fresh, invalidated after persistence mutations, and exposes built-in/custom WAV cue replace/remove/blob-read helpers |
| `lib/audio-cue.js` | `lib` | Board prompt-sound helper that decodes uploaded WAV files, downmixes and resamples them to 16 kHz mono PCM, and emits compact WAV bytes for app-local persistence plus appearance OTA |
| `lib/audio-cue.test.js` | `test` | Node coverage for board-safe WAV header constants and multi-channel downmixing before cue encoding |
| `lib/agent-appearance-config.js` | `lib` | Shared fixed-agent options, detected-agent normalization, localStorage per-channel appearance mapping, single-active-channel `enabled-agents` helpers, channel-switch warning predicates, and assignment lookup helpers for the dashboard and gallery |
| `lib/agent-appearance-config.test.js` | `test` | Node regression coverage for per-channel appearance assignment, single active desktop-channel behavior, channel-switch warnings, and unavailable-channel normalization |
| `lib/device-availability.js` | `lib` | Shared helper for resolving the current online board from bridge availability, preferring exact saved ids, then matching desktop ids, then a single online board for dev stale-binding walkthroughs |
| `lib/device-availability.test.js` | `test` | Node regression coverage for exact, same-desktop, single-online stale-binding, and unrelated multi-board availability resolution |
| `lib/desktop-pet-assignment.js` | `lib` | Shared gallery/dashboard workflow for setting a desktop pet, preserving per-channel appearance config, saving the single active channel, restarting Bridge, dispatching device follow-source binding over USB or MQTT, requiring USB for appearance changes. **修复**：旧版"USB 已连接且 localStorage 缓存说 appearance 未变就跳过 sync"会导致客户端 UI 与设备实际形象脱节（缓存乐观但设备实际可能是旧形象 / 上次 OTA 失败 / 设备被重启）；当前策略改为只有目标形象 ID 与当前显示形象不一致时才触发 `usb_sync_appearance`，真实换形象必须素材同步成功后才保存 Bridge 跟随渠道；若只是切换跟随且形象一致，则直接保存跟随渠道，不重推动画素材。 |
| `lib/desktop-pet-assignment.test.js` | `test` | Node regression coverage that the shared set-as-desktop-pet workflow saves exactly one active channel, mirrors it to Bridge enabledAgents, dispatches device follow binding, syncs assets only for real appearance changes, skips unchanged-appearance follow-switch re-pushes, explains hard sync failures while avoiding channel mutation, and blocks appearance changes without USB |
| `lib/builtin-appearances.js` | `lib` | Built-in appearance descriptors for bundled Westie/Terrier clips, including default Terrier done/error/waiting_user WAV prompt cues |
| `lib/builtin-appearances.test.js` | `test` | Node test coverage for Westie/Terrier built-in descriptor shape, Terrier default audio cue descriptors, and first-position merge behavior |
| `lib/appearance-preview.js` | `lib` | Pure media-selection helpers for gallery cards and family thumbnails, including Codex source-preview preference and source-image fallback descriptors |
| `lib/client-cache.js` | `lib` | Tiny async TTL cache primitive for client-side local reads and scan results with synchronous fresh-value peeking |
| `lib/client-cache.test.js` | `test` | Node coverage for TTL reuse, synchronous peeks, force refresh, explicit invalidation, and concurrent load coalescing |
| `lib/codex-community-import.js` | `lib` | Pure helpers for Codex pet modified-time snapshots, newest-first sorting, updated-pet detection, and pasted community import parsing |
| `lib/codex-community-import.test.js` | `test` | Node test coverage for community import snapshot diffing and URL/curl/CLI parsing |
| `lib/codex-pets-client.js` | `lib` | Cached Tauri command facade for Codex pet ffmpeg checks, scans, installs, and imports |
| `lib/codex-pets-client.test.js` | `test` | Node coverage for cached Codex pet listing and install-triggered invalidation |
| `lib/generation-task.js` | `lib` | Singleton avatar-generation task runner that survives wizard navigation, reports filtered custom-generation progress, persists partial successes, records the normalized thinking-model name, and surfaces concise provider failure summaries |
| `lib/generation-task.test.js` | `test` | Node regression coverage for avatar-generation failure message normalization, repeated-family error collapse, and provider-specific guidance |
| `lib/avatar-pipeline/families.js` | `lib` | Per-family first/last-frame animation grammar with playback mode, spatial path, subject-adaptive silhouette signature, concrete working keyboard/mouse/page props, larger waiting-user motion, touch-screen feedback semantics, anti-collapse guidance, and `CUSTOM_GENERATION_FAMILIES` that excludes importer-only directional `touch.right` / `touch.left` states from user-created full-avatar generation |
| `lib/avatar-pipeline/image-processing.js` | `lib` | Reference-image preprocessing that removes background, decontaminates semi-transparent alpha matte pixels before black compositing so pale subjects do not blend into dark fringes, centers the cutout on a black 4:3 canvas, and emits an even-dimension PNG for video generation |
| `lib/avatar-pipeline/image-processing.test.js` | `test` | Node regression coverage for 4:3 black-canvas compositing geometry and alpha matte decontamination before black-background compositing |
| `lib/avatar-pipeline/pipeline-defaults.js` | `lib` | Central avatar generation defaults for Ark-safe 400x300 preprocessing and 5-second fast provider task settings |
| `lib/avatar-pipeline/pipeline-defaults.test.js` | `test` | Node regression coverage for fast Ark-safe 400x300 5-second provider config resolution |
| `lib/avatar-pipeline/prompts.js` | `lib` | Thinking-model prompt builder and response normalizer that carries subject-adaptive first/last-frame motion grammar, medium-to-large middle-motion requirements, required marker props, and touch-screen feedback semantics into video-generation prompts |
| `lib/avatar-pipeline/prompts.test.js` | `test` | Node regression coverage that keeps welcome as a one-shot entrance, requires per-family marker props, prevents core states collapsing into the same pose, enforces concrete working keyboard/mouse/page props plus larger waiting-user motion, and keeps touch variants as screen-touch close-ups |
| `lib/avatar-pipeline/provider-config.js` | `lib` | Shared video-provider catalog and localStorage persistence helpers used by the full custom avatar wizard and the single-state retry UI so API keys, model names, fast-generation defaults, and advanced custom-provider settings stay in one contract |
| `lib/avatar-pipeline/provider-config.test.js` | `test` | Static Node coverage that the shared provider-config module owns the localStorage key, default provider id, Volcengine defaults, and fast-generation default normalization |
| `lib/avatar-pipeline/run.js` | `lib` | Avatar generation orchestrator that applies fast low-resolution config before image preprocessing, normalizes Volcano thinking-model fallback away from Seedance video models, runs Ark Responses prompt generation for filtered custom-generation families, and exposes `runSingleFamilyVideo` for a user-prompted one-state retry without regenerating the full family set |
| `lib/avatar-pipeline/run.test.js` | `test` | Node regression coverage that prevents Volcano Seedance video model names from being reused as thinking models, keeps custom full generation off importer-only directional touch states, and keeps single-state retry manifests limited to the selected family prompt |
| `lib/avatar-pipeline/system-prompt.js` | `lib` | Chinese system prompt for the thinking model, including subject-type detection, subject-adaptive anatomy, medium-to-large first/last-frame family motion, bounded welcome greeting semantics, working keyboard/mouse/page prop constraints, screen-touch feedback semantics, and controlled prop use |
| `lib/avatar-pipeline/thinking-model.js` | `lib` | Volcengine Ark Responses prompt-generation adapter with requested-family prompt building, Doubao 2.0 multimodal default request building, DeepSeek text-only fallback protection, and response-text extraction |
| `lib/avatar-pipeline/thinking-model.test.js` | `test` | Node regression coverage for Ark `/api/v3/responses` Doubao multimodal and DeepSeek text-only request shapes without endpoint IDs |
| `lib/avatar-pipeline/providers/volcano.js` | `lib` | Volcengine Ark `/api/v3/contents/generations/tasks` provider adapter with model-specific Seedance 1.5 prompt-flag payloads, Seedance 2.0 first/last-frame payloads, safe submit diagnostics, ModelNotOpen account guidance, polling, and MP4 download |
| `lib/avatar-pipeline/providers/volcano.test.js` | `test` | Node regression coverage for Ark v3 model-specific first/last-frame payload shape, Seedance model names, safe diagnostics, and ModelNotOpen account guidance |
| `styles.css` | `style` | Shared styling for the clean desktop shell, shared `ui-field`/`ui-control` form primitives, fixed-height first-level device/pet-album/component-center sidebar tabs, app-like responsive component store search, cards, generated/custom draft install/delete action rows, unified component-center builtin/draft/create card geometry with adaptive complete previews that follow the card frame and un-clamped card copy, fixed-slot component-center negative-screen internals with top-left title/top-right status/bottom progress parity, replacement modal, Step 2 generation guidance, and optional button-function settings, inline expanded single desktop-pet channel assignment panel with non-blocking USB appearance-sync progress, compact single-column bound-device/runtime status density with manual USB serial rescan action and wrapped independent USB/WiFi chips, board-voice assistant card with a compact shared-control session select shell, mock text injection editor, device voice status banners, flattened audio controls, c-version-sourced larger black-screen board-button SVG map with non-overlapping split callout labels, direct compact per-button function editor rows, USB OTA note, compact dashboard formosa picker modal, USB WiFi apply modal card/footer/stage chrome, connection-gated dashboard modal appearance picker actions, two-method setup wizard with final single-channel confirmation and default appearance preview, runtime status experience, compact gallery action buttons, fixed-size reference upload dropzone, fit-width centered pet-album previews with Codex-specific contain previews, state-card audio badges, detail-page preview-first workspace, sticky current-state controls, horizontal state rail, contextual info drawer, WAV cue controls, compact single-state regeneration CTA button/modal chrome, audio-OTA reminder button/banner, non-overlapping card metadata badges, Codex import thumbnails, gallery channel-selection modal, and source-first community import method blocks |
| `shell/` | `folder` | Cross-page shell primitives including bridge-profile active channel hydration — see `shell/.folder.md` |
| `dashboard/` | `folder` | Device-page subcomponents — see `dashboard/.folder.md` |
<!-- vibe-folder-map:end -->
