# Folder Plan: ref/src/component-center

## Architecture
- 2026-08-07 mapping-aware game guidance: every game card and detail modal
  displays its goal plus a compact “怎么玩” summary derived from routed package
  bindings. Directional joystick actions are grouped, edited mappings update
  the detail copy immediately, and exit is described as following global settings.
- 2026-08-07 v4 scene preview: the shared Canvas renderer loads validator-
  approved PNG data URLs, crops bounded horizontal animation frames, renders
  the expanded semantic shape set, and keeps `clean` imagery smooth while
  retaining nearest-neighbor `pixel` behavior and v3/v1 compatibility.
- 2026-08-07 button-config model v9 keeps component controls repeatable while
  advancing the shared PC/firmware contract for joystick Up/Down navigation.
- 2026-08-07 input-cache v8 update: the P4 defaults return to SW1 short Confirm
  and SW3 short Back. Migration recognizes only the complete shipped v7 map;
  component actions may use SW1 while the live `page_back` event remains
  reserved for global exit.
- 2026-08-06 optional global-exit synchronization: button validation now waits
  for the exact connected P4 configuration, reserves every repeated `page_back`
  mapping, treats no mapping as a valid user choice, and blocks on query failure
  instead of racing with a fabricated SW1 default. Obsolete full-map/snapshot
  compatibility helpers were removed because component installs remain package-owned.
- 2026-08-06 multi-slot sync-copy clarification: the component confirmation no
  longer claims a newly synchronized package will replace a "current"
  component. Components have independent installed identities; destructive
  replacement copy remains only for an explicitly detected single-slot device.
- 2026-08-05 global-exit reservation: the component editor receives the live
  P4 `page_back` control, labels it as global exit, disables it for component
  actions, and omits all legacy package navigation bindings. Component actions
  remain editable, but exit is no longer package-owned or written during sync.
- 2026-08-05 board-default migration guard: the shared button helper detects
  the complete shipped model-v6 binding set and swaps only its SW1/SW3 defaults.
  A board with any custom gesture is returned unchanged.
- 2026-08-05 input-cache v7 update: the global P4 defaults now map SW1 short
  to Back (Cancel) and SW3 short to Confirm/Enter. Model-v6 cache entries
  migrate only values matching the former defaults, and the component editor's
  system-navigation guidance now identifies SW1 as the global return key.
- 2026-08-04 input-cache v6 update: the global P4 default makes SW1 short
  press Confirm and keeps joystick-center long press unbound. Version-5 cache
  entries migrate only values matching the previous defaults, preserving
  unrelated user mappings and component-owned controls.
- 2026-08-04 joystick component-input update: component editing and package
  validation accept explicit `joystick.up`/`joystick.down` actions. Left/right
  and center keep their legacy rotary event identifiers, so prior `.clawpkg`
  files remain installable and run unchanged. Button-config model v5 includes
  the complete joystick-aware compatibility snapshot without overwriting the
  device's global navigation bindings.
- 2026-08-03 unified library update: Component Center removes the separate
  device browser and deferred batch plan. Live board inventory is still the
  source of truth, but each library card now owns one immediate two-state
  action: sync when absent, remove when present. Formal-local trash is a
  device-first transaction when installed, so a failed board ACK preserves the
  PC source. Unknown board-only packages are folded into the same grid as
  read-only, removable records. Redundant library title/search/count chrome and
  top-right preview status overlays are removed; type filters and bottom device
  actions remain.
- 2026-07-30 Windows preview-performance update: shared component game
  previews run their blocks/snake/flappy timers only while the document is
  visible and the individual device screen is inside a bounded viewport margin,
  preventing offscreen library cards from continuously rebuilding pixel grids.
  Visible dynamic grids draw into one pixelated Canvas instead of mutating up to
  160 individual DOM cells per frame.
- 2026-07-28 button-model v4 update: the shared device-button cache advances
  with the revised global navigation defaults (SW2 Component Center, SW3 Back,
  encoder-long unbound) so version-3 defaults migrate without replacing
  unrelated user mappings or component-owned controls.
- 2026-07-28 button-model v3 update: the shared device button cache version advances so the dashboard can migrate version-2 defaults to the simplified P4 map. Component-owned `miniapp_action` compatibility helpers remain package-internal and are no longer exposed by the global button dropdown.
- 2026-07-28 library ordering update: `library-order.js` normalizes ISO/numeric
  timestamps for stable newest-first sorting of generated packages. Builtins
  now retain their explicit product order ahead of that generated list.
- 2026-07-28 native-game field refinement: animated blocks/snake/flappy
  previews remove the thick pale playfield card and heavy grid shadow in favor
  of a palette-tinted field, 1px dark borders, and two pixels of inset. The
  Flappy preview path also mirrors the board's gentler one-tap climb.
- 2026-07-28 widget visual-parity update: the client preview and P4 renderer
  resolve pixel-tool accents from the same safe palette meanings
  (`candy=amber`, `sunset=coral`, `mint=aqua`, `arcade=violet`) and resolve
  semantic icons from `visualSprite`, so a synchronized package no longer
  changes its visual identity merely because it moved from React to firmware.
- 2026-07-27 modal-safety update: the preview modal traps Tab, closes on Escape, restores the opener, explains single-slot replacement without claiming the old package remains, and turns unknown device-only packages into read-only details with no install action.
- 2026-07-27 component button-scope update: component install/update writes the customized bindings into the package only. The modal explains that those mappings activate only while the component is open and do not overwrite Pet Manager's persisted encoder enter/back navigation.
- 2026-07-27 enabled-component lifecycle update: the library keeps the currently enabled item visible, resolves every card from explicit `kind` metadata before the legacy `gameType` fallback, and exposes semantic `aria-current` state only for an exact, target-verified source. Device removal and formal-local dual deletion both freeze the exact target in a focus-safe confirmation dialog.
- 2026-07-27 detail-button unification: the current-component hero and component preview/editor now render every conventional action through the shared shell `Button` primitive. Close, reset, cancel, install, and save each use the hierarchy documented in `ref/design.md`; install state also owns accessible loading and prevents backdrop dismissal.
- 2026-07-27 detail-layout refinement: the component preview/editor now uses a stable overview/workspace split, keeps the device preview and component summary together, isolates button mapping in its own configuration surface, consolidates conditional risk into a semantic impact bar, and adds a contextual confirmation footer. It collapses progressively at 840px and 680px without hiding the primary action.
- 2026-07-27 seven-gesture update: component button choices now expose SW1/SW2/SW3 short press only plus encoder short/long/left/right. Built-in `page_main` exits moved from SW3 long press to encoder long press, while full downlinks explicitly disable legacy SW long/hold events.
- 2026-07-23 encoder-long snapshot update: the complete component button map now mirrors the encoder long-press event into the dashboard's `p4_encoder_long` row after an ACKed install.
- Scope: ref/src/component-center
- Function: Sub-components for the Component Center page: reusable component previews/cards and the final sync/editor modal. The preview gives bounded blocks/snake/flappy grids a game-first field with a compact score HUD and retains semantic pixel sprites. Cards expose live sync/remove state directly; the modal edits package-owned inputs, reserves device-level navigation, exposes device removal and PC/dual deletion, and blocks unsupported firmware or conflicting bindings.
- Sync: if this folder changes, update this file immediately.
- Reuse: Card imported from `../shell/Card.jsx`; CSS tokens from `../styles.css`; button options and the exact shipped-default migration stay centralized in `button-config.js`.

## Files
| File | Pos | Function |
|---|---|---|
| `NowShowingHero.jsx` | `component` | Top hero card for the current record: device preview, source/type chip, exact-target “已启用” or legacy “上次启用 · 未确认设备” state, compact mappings, editor, safe device removal, and change action. `aria-current` and removal exist only for verified targets; an externally removed draft keeps its exact source identity instead of falling through to a same-id builtin. |
| `NowShowingHero.test.js` | `test` | Static Node coverage for verified-only current/removal semantics, unverified history copy, Card/preview reuse, explicit kind, bindings/editor, built-in badges, disabled guard, and passive empty state. |
| `DeviceScreenPreview.jsx` | `component` | Shared negative-screen preview renderer used by built-in/formal-library cards, current-component hero, and install modal. It mirrors bounded scene motion, fifteen semantic silhouettes, and safe sprite-sheet animation on Canvas, supports distinct modern-clean/pixel shells and eight palettes, retains blocks/snake/flappy compatibility, and keeps the P4-matched instrument layout for pixel tools. |
| `DeviceScreenPreview.test.js` | `test` | Static Node coverage for classic compatibility, clean/pixel game themes, bounded semantic scene silhouettes and sprite plumbing, palette variants, board-shared pixel-tool meanings, and viewport-bounded Canvas animation. |
| `CandidateCard.jsx` | `component` | Compact card for the unified library: unobstructed device-screen content, explicit game/tool and builtin/custom metadata, goal plus mapping-aware “怎么玩” copy for games, semantic installed/current state, exact bottom two-state sync/remove action, and formal-local dual-delete icon. |
| `CandidateCard.test.js` | `test` | Static coverage for source/kind/installed/enabled variants without preview status overlays, mapping-aware game guidance, exact bottom sync/remove copy, dual deletion, screen reuse, focus-safe geometry, and complete copy. |
| `library-order.js` | `lib` | Normalizes ISO/numeric component creation timestamps and returns a stable newest-created-first library order across builtins and drafts. |
| `library-order.test.js` | `test` | Node coverage for mixed timestamp sources, newest-first ordering, invalid dates, and stable ties. |
| `ComponentPreviewModal.jsx` | `component` | Accessible immediate-sync/current editor with exact-source identity, focus trap/restore and Escape handling, sticky screen preview, live mapping-aware game instructions, package-owned input mapping, PC/dual deletion, single-slot sync-impact warning, read-only device-package details, direct removal, and shared Button hierarchy. |
| `ComponentPreviewModal.test.js` | `test` | Static coverage for layout/responsiveness, focus lifecycle, mapping-aware game guidance, device-only read-only state, single-slot consequences, kind labeling, editable bindings, local deletion, device removal, conflicts, and install guards. |
| `binding-labels.js` | `lib` | Formats screen, SW1-SW3, joystick directions, and center press bindings, groups routed actions into concise game-play guidance, accepts legacy encoder/knob event aliases, and filters package-authored system navigation. |
| `binding-labels.test.js` | `test` | Direct Node coverage for grouped joystick guidance, physical-button labels, global-exit copy, and legacy navigation filtering. |
| `button-config.js` | `lib` | Owns the screen/SW1-SW3/four-direction joystick component input-option catalog, exact shipped-v6 board-default migration, and authoritative optional global-exit lookup; active installs keep bindings package-owned. |
| `button-config.test.js` | `test` | Node coverage for unique component-control options, label resolution, exact-default-only board migration, and optional authoritative global-exit lookup. |
