# 固件目录

0.7.50-p4 使用统一 4M UART 配置，供 Windows/macOS 客户端及完整烧录包共用。
Windows PlatformIO 环境仅作为兼容别名继承默认环境，不再覆盖通信速率。

- `main/`：设备运行逻辑。
- `components/`：板级驱动及适配组件。
- `tools/`：内置组件处理及完整出厂镜像生成工具。
- `tests/`：主机测试与手动设备测试。

内置素材和组件从 `../pc/` 读取。构建与烧录方法见 [README.md](README.md)。
