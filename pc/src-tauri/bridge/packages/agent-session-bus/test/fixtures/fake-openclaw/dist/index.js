// Stub entry point for the fake openclaw package. The real openclaw exposes
// the runtime here; the adapter only needs `dist/plugin-sdk/agent-runtime.js`
// to exist so this file is intentionally minimal.
export const STUB = true;
