interface:
  display_name: "petui"
  short_description: "生成 PNG 优先且通过完整玩法自测的 P4 组件"
  default_prompt: "Use $petui to generate, validate, gameplay-test, and publish a polished PNG-first P4 game or tool component."

policy:
  allow_implicit_invocation: true
